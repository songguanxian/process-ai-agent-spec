from pathlib import Path
import os
import shutil

from docx import Document
from docx.enum.text import WD_LINE_SPACING
from docx.oxml import OxmlElement
from docx.oxml.ns import qn
from docx.shared import Pt


SOURCE = Path(os.environ["DOCX_PATH"])
OUTPUT = Path(os.environ["OUTPUT_DOCX"])


def insert_paragraph_after(paragraph, text="", style=None):
    new_p = OxmlElement("w:p")
    paragraph._p.addnext(new_p)
    new_para = paragraph._parent.add_paragraph()
    new_para._p = new_p
    if style:
        new_para.style = style
    if text:
        new_para.add_run(text)
    return new_para


def set_spacing(paragraph):
    pf = paragraph.paragraph_format
    pf.space_before = Pt(3)
    pf.space_after = Pt(3)
    pf.line_spacing_rule = WD_LINE_SPACING.ONE_POINT_FIVE


def set_heading_spacing(paragraph):
    pf = paragraph.paragraph_format
    pf.space_before = Pt(9)
    pf.space_after = Pt(4)


def add_after(anchor, entries):
    current = anchor
    for style, text in entries:
        current = insert_paragraph_after(current, text, style)
        if style and "Heading" in style:
            set_heading_spacing(current)
        else:
            set_spacing(current)
    return current


def mark_font(doc):
    for paragraph in doc.paragraphs:
        for run in paragraph.runs:
            if run.text:
                run.font.name = "宋体"
                run._element.get_or_add_rPr().rFonts.set(qn("w:eastAsia"), "宋体")


OUTPUT.parent.mkdir(parents=True, exist_ok=True)
shutil.copy2(SOURCE, OUTPUT)

doc = Document(str(OUTPUT))

anchor = None
in_scene3 = False
for para in doc.paragraphs:
    text = "".join(para.text.split())
    if text.startswith("场景三：AI视觉识别/OCR与表单自动填报"):
        in_scene3 = True
    elif in_scene3 and text == "已知信息":
        break
    elif in_scene3 and text == "功能明细":
        anchor = para

if anchor is None:
    raise RuntimeError("未找到场景三的功能明细插入点")

entries = [
    ("Heading 3", "场景三细化实施口径"),
    (
        None,
        "本场景首批定位为“AI识别提取 + 候选匹配 + 人工确认 + 自动填充/挂接附件”的半自动闭环。AI识别结果必须经用户确认后生效，首批不承诺无需人工干预的全自动填报。",
    ),
    ("Heading 3", "首批识别范围与字段"),
    (
        None,
        "App端首批支持白板照片、责任登记卡照片，支持现场拍摄、相册上传、旋转和裁剪。Web端首批支持图片和PDF上传，可从AI侧边栏/全局入口或表单页面入口发起。",
    ),
    (
        None,
        "Web端PDF首批支持可OCR的扫描件或图片型PDF，可选择全部页、指定页或前N页识别；复杂报告类、多附件混排类PDF只提取候选字段，不保证完整结构化。",
    ),
    (
        None,
        "首批识别字段包括桩号、工程部位、施工节点、施工单位、合同段、工程名称、单位工程、人员信息、附件名称等。其中桩号、工程名称/合同段、工程部位/结构、责任人为核心字段；附件名称、施工节点、人员附属信息为辅助字段，辅助字段缺失不阻断流程。",
    ),
    ("Heading 3", "桩号标准化与候选匹配"),
    (
        None,
        "桩号需先标准化，支持K12+345、K12+345-K12+678、多桩号、桩号范围、左右幅、上下行、匝道等扩展信息。匹配时先用工程名称、合同段、施工单位缩小项目范围，再用标准化桩号区间匹配实体工程桩号库和结构库。",
    ),
    (
        None,
        "候选排序依据包括项目、合同段、桩号范围、部位/结构名称等命中程度。完全命中者排前，部分命中者作为候选但不自动绑定。多桩号或跨结构场景首批允许一条识别结果关联多个桩号或结构，但主结构/主部位必须由用户确认。",
    ),
    (
        None,
        "库内无匹配时，不自动新增项目、结构或人员基础库，只允许人工绑定已有对象或补录文本，并标记为“未匹配”。",
    ),
    ("Heading 3", "确认页与置信状态"),
    (
        None,
        "App/Web上传后均先进入识别确认页，确认后再跳转表单、自动填充并挂接附件。确认页包括原图/PDF预览、字段确认、候选匹配和操作区。",
    ),
    (
        None,
        "预览区支持旋转、裁剪、放大、切换页/图片；首批可展示字段来源高亮，但不强制复杂标注。字段区按核心字段和辅助字段分组展示识别值、置信状态和人工修改入口。匹配区展示项目、合同段、结构、人员候选及匹配依据，例如命中桩号范围、名称相似、合同段一致。操作区提供确认填表、重拍/重传、手工补录、人工绑定、作废任务等动作。",
    ),
    (
        None,
        "置信状态分为四档：一是可一键采用，核心字段置信度高且项目/合同段/结构/人员匹配唯一，确认页默认选中候选，用户点击确认即可填表；二是需人工确认，字段基本识别出来但存在多个候选或部分字段中等置信，用户必须选择候选或修正字段后继续；三是需补录绑定，核心字段缺失、桩号无法标准化或库内无明确对象时，允许手工补录字段并绑定已有项目/结构；四是建议重拍/重传，图片模糊、遮挡严重、PDF不可OCR或核心字段大面积缺失时提示重拍/重传，用户仍可手工录入。首批不写死百分比阈值，先按状态和行为规则执行，阈值后续配置化调整。",
    ),
    ("Heading 3", "识别批次、结果组与归档"),
    (
        None,
        "附件和OCR结果按“识别批次”统一归档。一次拍摄/上传可包含多张图片或一个PDF，生成一个识别批次。批次下保存原始文件、裁剪图、页码/图片序号、OCR文本、字段结果、匹配候选和质量检测结果。",
    ),
    (
        None,
        "用户确认后，识别批次整体挂接目标表单，并关联项目、结构、部位、责任人等对象。多图/多页字段冲突时，不自动合并覆盖，确认页提示冲突，由用户选择主结果或逐字段修正。",
    ),
    (
        None,
        "批次内可拆分为多个“结果组”，系统可按页、图片、工程部位/桩号/责任卡组织。每个结果组可分别确认字段、候选对象、目标表单，并生成或关联不同表单。首批自动拆分只做按页/按图片级别；按业务语义自动拆分作为后续增强，允许用户手工拆分/合并结果组。",
    ),
    (
        None,
        "允许有权限用户后续改绑附件/识别批次，但必须记录改绑人、时间、改绑前后对象和原因。系统需提供OCR识别记录台账，可按项目、合同段、结构、上传人、识别状态、是否人工修改查询。",
    ),
    ("Heading 3", "异步任务与状态机"),
    (
        None,
        "OCR/视觉识别统一采用异步任务 + 状态机，App/Web只负责上传、查看状态和确认结果。任务状态至少包括待处理、识别中、待确认、已确认填报、识别失败、已作废。",
    ),
    (
        None,
        "App端弱网时先保留本地待上传记录，上传成功后生成识别任务。Web端图片可较快返回，但PDF、多页、多图走异步；用户可离开页面，稍后从任务列表或消息提醒进入确认页。失败、超时、服务异常允许重试；重复上传同一附件时系统提示可能重复，但不强行拦截。",
    ),
    (
        None,
        "识别任务与业务表单解耦，只有用户确认后才创建或更新表单并挂接附件。识别失败、超时、质量差或用户跳过识别时不阻断业务，允许手工选择项目/结构/表单并挂接附件，状态可转为“人工处理完成”，并纳入台账统计。",
    ),
    ("Heading 3", "权限与表单填报规则"),
    (
        None,
        "具备对应项目/合同段权限的App/Web用户可以上传图片/PDF并发起识别任务。上传人可查看自己的识别任务；项目管理员、监管人员或授权角色可查看项目/合同段范围内全部任务。只有对目标项目、结构、表单有填报权限的用户，才能确认识别结果并自动填表。",
    ),
    (
        None,
        "已确认批次的改绑或作废仅限项目管理员、监管负责人或指定授权角色，并要求填写原因。责任人字段优先按识别文本匹配人员库；无法匹配时可给出当前登录人或项目角色候选，但必须人工确认。",
    ),
    (
        None,
        "App白板/责任登记卡和Web图片/PDF共享基础字段模型，但允许按来源类型启用不同字段模板。系统根据施工节点、工程部位、监管工程类型、附件来源等信息推荐目标表单类型；推荐唯一且置信度高时可默认选中，存在多个可能表单时必须由用户选择。",
    ),
    (
        None,
        "识别字段到业务表单字段的映射按表单类型配置，例如“工程名称→项目名称”“工程部位→部位/关联结构”“人员信息→责任人/施工员/质检员”。填表失败时，识别批次保留为“待确认/已确认未填表”状态，用户可重新发起填表。",
    ),
    (
        None,
        "自动填报前先查重：按项目、合同段、结构/部位、施工节点、表单类型、日期或识别批次查找可能已有表单。若存在同一业务对象的未提交草稿，默认跳转草稿并填充缺失字段、挂接附件，不直接新建；若存在已提交或已审核记录，默认只允许追加附件或生成补充记录，不自动覆盖原字段；无匹配记录时新建表单草稿。目标表单已有人工填写内容时，不直接覆盖，只提示“使用识别结果替换/保留原值”。",
    ),
    ("Heading 3", "Web入口差异"),
    (
        None,
        "表单页上传属于“上下文已知”，当前表单、项目、合同段、结构作为默认上下文，识别结果优先用于补全当前表单。侧边栏/全局入口上传属于“上下文未知”，需用户先选择或由系统识别推断项目/合同段，再进入确认页。已知上下文作为候选匹配强约束或高权重条件，但用户可在权限范围内改选。无论从哪里上传，最终都进入同一识别任务、确认快照和归档机制。",
    ),
    ("Heading 3", "架构边界与数据落库"),
    (
        None,
        "架构按“AI负责识别提取，业务系统负责匹配、确认、填表和归档”划分。AI服务职责是接收图片/PDF，返回标准化字段、置信度、OCR文本、字段来源页/区域和图片质量判断。业务系统职责是基于识别字段查询项目库、桩号库、结构库、人员库，生成候选匹配，承载确认页、权限控制、表单跳转、字段映射和附件归档。",
    ),
    (
        None,
        "项目、合同段、结构、人员匹配逻辑放在业务系统，因为依赖业务库、权限和表单规则。识别批次、确认记录、表单字段和附件挂接关系最终由业务系统落库管理。AI服务返回统一JSON结构，业务系统可配置字段映射和阈值，不依赖模型内部实现。",
    ),
    ("Heading 3", "确认快照、审计与配置版本"),
    (
        None,
        "每次人工确认固化为“确认快照”。快照需保存AI原始结果、用户确认结果、匹配对象快照和填表结果快照。AI原始结果包括字段值、置信度、OCR文本、来源页/区域、质量判断；用户确认结果包括最终确认或修改后的字段值及修改原因；匹配对象快照包括当时选中的项目、合同段、结构、人员、候选列表和匹配依据；填表结果快照包括填入哪个表单、哪些字段被填充、哪些字段被跳过、附件挂接到哪里。",
    ),
    (
        None,
        "确认前后差异可用于模型/规则优化，但需按权限控制，并对人员等敏感信息做脱敏或最小化使用。OCR字段模板、匹配阈值和表单映射做成可配置、可版本追溯的管理项。每次识别任务和确认快照记录当时使用的模型版本、字段模板版本、匹配规则版本和表单映射版本。只有系统管理员或业务配置管理员可修改配置，变更需记录修改人、时间和说明。",
    ),
    ("Heading 3", "App拍照质量引导"),
    (
        None,
        "App拍照页提供取景框、白板/登记卡完整性提示、横竖屏/距离/光线提示。上传前或上传后快速检测模糊、反光、倾斜、遮挡、文字过小、主体不完整等问题。质量不达标时提示重拍并说明原因，但不强制拦截，允许用户继续上传并进入手工补录。用户可在上传前裁剪、旋转，减少背景干扰。质量检测结果保存到识别任务，作为失败原因分析和后续优化依据。",
    ),
    ("Heading 3", "验收口径、首批边界与后续增强"),
    (
        None,
        "首批验收以“流程可闭环 + 关键字段可用率 + 人工修正可追溯”为主，不要求OCR全自动准确。验收重点包括上传、识别、候选匹配、人工确认、自动填表、附件挂接、台账追溯全流程闭环。关键指标包括关键字段识别可用率、候选匹配命中率、人工修正率、确认后自动填表成功率、附件错挂纠正率。人工修改时可标记原因，例如OCR识别错误、候选匹配错误、业务选择调整、基础库缺失。",
    ),
    (
        None,
        "首批不做：无需人工确认的全自动填报、任意版式PDF全文结构化、OCR自动新增项目/结构/人员基础库、跨项目无权限识别归档。图片约束为主体清晰完整、文字无遮挡；模糊、反光、倾斜严重时进入重拍或手工录入。PDF约束为支持可OCR的扫描件或图片型PDF；复杂报告类、多附件混排类PDF只提取候选字段，不保证结构化完整。自动匹配依赖项目库、合同段、实体工程桩号库、结构库、人员库完整性和规范性；库缺失时只能人工补录或绑定。后续增强项包括全自动归档、批量PDF自动拆分、多表单自动生成、复杂手写识别、模型主动学习等。",
    ),
]

add_after(anchor, entries)
mark_font(doc)
doc.save(str(OUTPUT))
print(OUTPUT)
